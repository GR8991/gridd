# Streamlit Secrets Configuration
# Save this file as .streamlit/secrets.toml in your project root

# Mock API Configuration (for local development)
API_BASE_URL = "http://localhost:5000"

# ERCOT API Configuration (use when you have credentials)
# Uncomment and fill in when you get your ERCOT API credentials
# ERCOT_SUBSCRIPTION_KEY = "your_subscription_key_here"
# ERCOT_ID_TOKEN = "your_id_token_here"

# Optional: Add more configuration as needed
DEBUG_MODE = true
CACHE_TTL = 300  # Cache time-to-live in seconds
